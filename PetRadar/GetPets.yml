info:
  name: GetPets
  type: http
  seq: 1

http:
  method: GET
  url: http:localhost:500/api/petradar
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
